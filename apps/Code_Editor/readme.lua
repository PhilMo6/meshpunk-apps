return { body = [[
An on-device Lua editor, for writing and fixing scripts without a PC.

Browse to a file and Edit it, or New File to start one. Run executes the file you are editing and shows a Runtime Error with the message if it fails, so you can fix it and run again without leaving.

This is how you can write an app on the device itself: an app is a folder with a main.lua in it, under lua/apps on either drive.]] }
